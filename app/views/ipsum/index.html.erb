<%= image_tag('fabricipsum.png', :class => "logo") %>

<h1 class="center-text logo-text">Fabric Ipsum</h1>

<section class="intro-wrapper">
  <div>
    <p>If you are looking for fabric auto-generated text to fill up your web pages, you've come to the perfect place. Who wants a boring ipsum? Not me! So, let's add some character to all the things with fabric! Fabric Ipsum is a fabric word generator, covering all words that are fabric related.</p>
  </div>
</section>

<%= form_with url: generate_path, local: true do |f| %>
  <p class="select-sentence">
    I want <%= f.select 'num', (1..5) %> paragraphs of fabric placeholder text.
  </p>

  <p class="submit-btn center-text">
    <%= f.submit 'Fabricfy!', data: { disable_with: false }, class: 'button' %>
  </p>
<% end %>

<div class="ipsum-wrapper" id="ipsum">
  <% @paragraph_arr.each do |paragraph| %>
    <p><%= paragraph %></p>
  <% end %>
</div>

<% if @paragraph_arr.present? %>
  <button class="button clipboard-btn"
          data-clipboard-action="copy"
          data-clipboard-target="#ipsum">
    Select All Text
  </button>
<% end %>
